# Public, build-time-inlined config (NEXT_PUBLIC_* is exposed in the client bundle).
# Committed on purpose: a static export inlines these at `next build`, so the deploy
# pipeline (Cloudflare / clean checkout) must have them even though .env.local is gitignored.
# These are not secrets — they already ship to every visitor's browser.
NEXT_PUBLIC_API_URL=https://script.google.com/macros/s/AKfycbx2o7ogvuFid_yAcdVGuCBoG7koCqQ9Kgt3WGmejRxKdHxSXnzQR68wRHPE2_6QcKcL/exec
NEXT_PUBLIC_ADSENSE_CLIENT=ca-pub-4427649049615669
